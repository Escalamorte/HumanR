class NetSal {
        double calcSal (double gross) {
            double net;
            double minSal = 200;
            double maxSal = 8000;

//            if (gross > maxSal) {
//                net = gross - ((gross - maxSal) * 0.14) - ((gross - minSal) * 0.3) - 6 - (gross * 0.5) ;
//            } else {
//                net = gross * 0.14;
//            }
            return gross;
        }
}
